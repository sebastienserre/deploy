on:
  push:
    branches:
      - master
name: Publish Website
jobs:
  FTP-Deploy-Action:
    name: FTP-Deploy-Action
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: FTP-Deploy-Action
        uses: SamKirkland/FTP-Deploy-Action@3.0.0
        with:
          ftp-server: sft://ns3044267.siegler-informatique.fr:61212/var/www/thivinfo.com/test-deploy
          ftp-username: sebastienserre
          ftp-password: ${{ secrets.FTP_PASSWORD }}
          ARGS: --verbose --continue --exclude-glob=.git*/** --exclude-glob=.git** --delete-excluded --delete --ignore-time